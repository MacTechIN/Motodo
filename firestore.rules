rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }

    // Helper to check if user belongs to a specific team
    function isTeamMember(teamId) {
      return isAuthenticated() && (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.teamId == teamId);
    }

    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }

    match /teams/{teamId} {
      allow read: if isTeamMember(teamId);
      allow create: if isAuthenticated();
      allow update: if isTeamMember(teamId) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    match /todos/{todoId} {
      // Create: Anyone authenticated can create
      allow create: if isAuthenticated();

      // Read: 
      // 1. Owner can see everything
      // 2. Team members can see only non-secret todos
      allow read: if isAuthenticated() && (
        request.auth.uid == resource.data.createdBy || 
        (isTeamMember(resource.data.teamId) && resource.data.isSecret == false)
      );

      // Update/Delete: Only owner
      allow update, delete: if isAuthenticated() && request.auth.uid == resource.data.createdBy;
    }
  }
}
